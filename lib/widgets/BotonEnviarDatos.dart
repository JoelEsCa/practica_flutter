import 'package:flutter/material.dart';

class BotonEnviarDatos extends StatelessWidget {
  const BotonEnviarDatos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}